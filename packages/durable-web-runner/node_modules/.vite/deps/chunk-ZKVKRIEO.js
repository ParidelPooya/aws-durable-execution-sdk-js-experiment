import {
  __esm,
  __export
} from "./chunk-LK32TJAX.js";

// src/crypto-polyfill.ts
var crypto_polyfill_exports = {};
__export(crypto_polyfill_exports, {
  createHash: () => createHash,
  default: () => crypto_polyfill_default,
  randomBytes: () => randomBytes,
  randomUUID: () => randomUUID
});
function createHash(algorithm) {
  return {
    update(data) {
      this.data = data;
      return this;
    },
    digest(encoding) {
      const hash = btoa(this.data || "").replace(/[^a-zA-Z0-9]/g, "").substring(0, 32);
      return encoding === "hex" ? hash : hash;
    },
    data: ""
  };
}
function randomBytes(size) {
  const bytes = new Uint8Array(size);
  crypto.getRandomValues(bytes);
  return bytes;
}
function randomUUID() {
  return crypto.randomUUID();
}
var cryptoPolyfill, crypto_polyfill_default;
var init_crypto_polyfill = __esm({
  "src/crypto-polyfill.ts"() {
    cryptoPolyfill = { createHash, randomBytes, randomUUID };
    crypto_polyfill_default = cryptoPolyfill;
  }
});

export {
  createHash,
  crypto_polyfill_exports,
  init_crypto_polyfill
};
//# sourceMappingURL=chunk-ZKVKRIEO.js.map
